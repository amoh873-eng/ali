@inherits LayoutComponentBase

@* 
MudRTLProvider: تفعيل اتجاه RTL للغة العربية في MudBlazor 8
(استُبدل RightToLeft بـ MudThemeProvider في v7 وأصبح MudRTLProvider في v8).
*@
<MudRTLProvider RightToLeft="true">
<MudThemeProvider Theme="_theme" IsDarkMode="false" />
<MudPopoverProvider />

@* 
MudDialogProvider: مزود النوافذ المنبثقة (Modals) لكل تطبيق MudBlazor.
*@
<MudDialogProvider />

@* 
MudSnackbarProvider: مزود الإشعارات (Toast) لعرض رسائل النجاح/الخطأ.
*@
<MudSnackbarProvider />

@*
MudLayout: التخطيط الرئيسي للصفحة.
يضم شريط علوي (AppBar) ومنطقة المحتوى (Body) مع الشريط الجانبي.
*@
<MudLayout>
    @* الشريط العلوي: الشعار + القائمة العلوية + الإشعارات + قائمة المستخدم *@
    <MudAppBar Elevation="0" Dense="true">
        @* اسم النظام (الشعار) *@
        <MudText Typo="Typo.h6" Style="font-weight:700; color:var(--erp-primary);">@Loc["AppName"]</MudText>

        @* شريط التنقل العلوي *@
        <TopNav />

        <MudSpacer />

        @* الإشعارات *@
        <MudIconButton Icon="@Icons.Material.Filled.Notifications" Color="Color.Inherit" Size="Size.Small" />

        @* قائمة المستخدم (تسجيل الخروج: عربي/إنجليزي) *@
        <MudMenu AnchorOrigin="Origin.BottomLeft" TransformOrigin="Origin.TopLeft">
            <ActivatorContent>
                <MudAvatar Color="Color.Primary" Size="Size.Small" Class="ml-3 mr-1" Style="cursor:pointer;">
                    <MudIcon Icon="@Icons.Material.Filled.Person" />
                </MudAvatar>
            </ActivatorContent>
            <ChildContent>
                <AuthorizeView>
                    <Authorized>
                        <MudMenuItem Href="/logout">
                            <MudIcon Icon="@Icons.Material.Filled.Logout" Class="mr-2" />
                            @Loc["Logout"]
                        </MudMenuItem>
                    </Authorized>
                    <NotAuthorized>
                        <MudMenuItem Href="/login">
                            <MudIcon Icon="@Icons.Material.Filled.Login" Class="mr-2" />
                            @Loc["Login"]
                        </MudMenuItem>
                    </NotAuthorized>
                </AuthorizeView>
            </ChildContent>
        </MudMenu>
    </MudAppBar>

    @* منطقة المحتوى - Body *@
    <MudMainContent>
        <MudContainer MaxWidth="MaxWidth.False" Class="px-4 py-2">
            @Body
        </MudContainer>
    </MudMainContent>
</MudLayout>
</MudRTLProvider>

@code {
    [Inject] private IStringLocalizer<SharedResources> Loc { get; set; } = default!;

    /// <summary>
    /// السمة الضوئية (Light): لون أساسي #2563EB وخلفية #F5F6F8 ونصوص داكنة.
    /// هذه القيم متزامنة مع متغيرات :root في app.css.
    /// </summary>
    private readonly MudTheme _theme = new()
    {
        PaletteLight = new PaletteLight
        {
            Primary = "#2563EB",
            Secondary = "#6B7280",
            Info = "#2563EB",
            Success = "#16A34A",
            Warning = "#D97706",
            Error = "#DC2626",
            Background = "#F5F6F8",
            Surface = "#FFFFFF",
            AppbarBackground = "#FFFFFF",
            AppbarText = "#1F2937",
            DrawerBackground = "#FFFFFF",
            DrawerText = "#1F2937",
            TextPrimary = "#1F2937",
            TextSecondary = "#6B7280",
            ActionDefault = "#1F2937",
            LinesDefault = "#E5E7EB",
            LinesInputs = "#CBD5E1",
            TableLines = "#E5E7EB"
        },
        LayoutProperties = new LayoutProperties
        {
            DefaultBorderRadius = "8px"
        },
        Typography = new Typography
        {
            Default = new DefaultTypography
            {
                FontFamily = new[] { "Cairo", "Tajawal", "Roboto", "Arial", "sans-serif" },
                FontSize = "0.9375rem"
            }
        }
    };
}
