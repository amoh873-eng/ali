@* يعيد التوجيه إلى صفحة تسجيل الدخول عندما لا يكون المستخدم مخوّلاً *@

@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/login", forceLoad: true);
    }
}